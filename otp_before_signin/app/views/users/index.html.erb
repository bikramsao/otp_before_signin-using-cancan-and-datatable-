<div class="page-header"><h1>Welcome!</h1></div>

<table id="usersTable",  data-source="<%= users_path(format: :json) %>">
  <thead>
    <tr>
      <th>Id</th>
    	<th>Name</th>
      <th>E-mail</th>
      <th>Role</th>
      <th>Action</th>
      <th>Action</th>
      <th>Status</th>
      <th>Block user</th>
    </tr>
  </thead>
  <tbody>
    <p>
      <% if can? :update, User %>
        <%= link_to 'Add new user',new_user_path,class:"btn btn-info" %>
      <% end %>   
    </p>
    <div id= "modal-window" class="modal fade" role="dialog" aria-hidden="true" aria-labelledby="myModalLabel" tabindex="-1"></div>
	</tbody>
</table>
